const nextConfig = {
    reactStrictMode: true,
    compiler: {
        styledComponents: true,
    },
    experimental: {
        optimizeCss: false, // Оптимизация CSS
    },
};

module.exports = nextConfig;
