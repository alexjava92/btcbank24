const nextConfig = {
    reactStrictMode: true,
    compiler: {
        styledComponents: true,
    },
    experimental: {
        optimizeCss: true,
        // Оптимизация CSS
    },
};

module.exports = nextConfig;
